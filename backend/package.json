{
  "name": "recanto-backend",
  "version": "1.0.0",
  "description": "Backend API for ReCanto English learning application",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "emulators": "firebase emulators:start --only auth,firestore",
    "dev:emulators": "concurrently \"npm run emulators\" \"npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^6.15.0",
    "firebase-admin": "^11.5.0",
    "jsonwebtoken": "^9.0.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "firebase-tools": "^11.24.0",
    "nodemon": "^2.0.20"
  }
}
